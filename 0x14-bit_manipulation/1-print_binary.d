#include "main.h"
/**
 * print_binary - binary representation of a number
 *@n: unsigned long int
 *Return: NULL
 */
 void print_binary(unsigned long int n)
 {
 if ( n != 0)
 {
	if ( n != 1)
	print binary(n != 1);
	_putchar(n & 1) + '0');
	}
	else
	{
		_putchar
	}
}
